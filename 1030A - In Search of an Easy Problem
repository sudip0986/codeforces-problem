#include <iostream>
using namespace std;

int main()
{
    int n;
    cin>>n;
    int a[n];
    
    bool f=0;
    for(int i=1; i<=n; i++)
    {
        cin>>a[i];
        if(a[i]==1)
        f=1;
    }
    
    if(f==0)
    cout<<"EASY";
    else
    cout<<"HARD";
}
