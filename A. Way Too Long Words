#include <iostream>

using namespace std;
int main()
{
   int t;
   cin>>t;
   t+=1;
while(t--)
{
    string w;
    getline(cin,w);
   if(w.size()>10)
   {
    string abriation=w[0]+to_string(w.size()-2)+w[w.size()-1];
    cout<< abriation<<endl;
   }
   else 
   cout<<w<<endl;
}
}
