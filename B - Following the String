#include <bits/stdc++.h>
using namespace std;
int main()
{
   int c;
   cin>>c;
   while(c--)
   {
       int n;
       cin>>n;
       int  a[n];
       for(int i=0; i<n; i++)
       cin>>a[i];
       
       vector<int>v(26,0);
       
      for(int i=0; i<n; i++)
      {
         for(int j=0; j<26; j++)
         {
              if(v[j]==a[i])
              {
                  char ch='a'+j;
                  cout<<ch;
                  v[j]++;
                  break;
              }
         }
      }
      
      cout<<"\n";
       
   }
    return 0;
}
